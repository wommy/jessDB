{
  "name": "jessDB",
  "version": "0.5.0",
  "scripts": {
    "start": "eleventy --serve --incremental",
    "build": "eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0-canary.5",
    "cheerio": "^1.0.0-rc.10"
  }
}
